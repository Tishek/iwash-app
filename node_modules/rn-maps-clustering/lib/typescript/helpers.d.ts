import React from 'react';
import type { Region } from 'react-native-maps';
import type { ClusterFeature, PointFeature, InputPointFeature } from './types';
export declare const isMarker: (child: React.ReactNode) => child is React.ReactElement<{
    coordinate: {
        latitude: number;
        longitude: number;
    };
}>;
export declare const markerToGeoJSONFeature: (marker: React.ReactElement<{
    coordinate: {
        latitude: number;
        longitude: number;
    };
}>, index: number) => InputPointFeature;
export declare const generateSpiral: (center: ClusterFeature, leaves: PointFeature[]) => Array<{
    longitude: number;
    latitude: number;
    index: number;
    centerPoint: {
        longitude: number;
        latitude: number;
    };
}>;
export declare const calculateBBox: (region: Region) => [number, number, number, number];
export declare const returnMapZoom: (region: Region, width: number) => number;
export declare const returnMarkerStyle: (points: number) => {
    width: number;
    height: number;
    fontSize: number;
    size: number;
};
//# sourceMappingURL=helpers.d.ts.map